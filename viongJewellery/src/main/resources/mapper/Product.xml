<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.viongJewellery.dao.ProductDAO">

  <select id="selectList" resultType="com.viongJewellery.entity.ProductEntity">
    SELECT PRODUCT_NUMBER,
           PRODUCT_NAME,
           PRODUCT_PRICE,
           PRODUCT_STCOK,
           PRODUCT_CONTENT,
           PRODUCT_IMAGE,
           PRODUCT_STATUS
    FROM PRODUCT
    WHERE 1=1
      AND (#{name} IS NULL OR PRODUCT_NAME = #{name})
  </select>

</mapper>
